[package]
name = "browser-harness"
version = "0.3.0"
description = "Playwright-based browser automation harness for need-singularity fleet. Fresh-context factory (mandate 1-6) + probe + selftest + real oauth-login (slot-isolated storageState persistence) + Mac→ubu1/ubu2/fleet SSH-pipe transport (lib/remote.cjs). See docs/oauth-login.md."
entry = "bin/browser-harness"
license = "MIT"

[package.dependencies]
node = ">=18.0.0"

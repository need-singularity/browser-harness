{
  "name": "browser-harness",
  "version": "0.1.0",
  "description": "Playwright browser automation harness — fresh-context factory + probe + selftest",
  "private": true,
  "bin": { "harness": "bin/harness" },
  "scripts": {
    "selftest": "node tests/selftest.cjs"
  },
  "dependencies": {
    "playwright-core": "^1.50.0"
  },
  "engines": { "node": ">=18.0.0" }
}

[package]
name = "browser-harness"
version = "0.2.0"
description = "Playwright-based browser automation harness for need-singularity fleet. Fresh-context factory (mandate 1-6) + probe + selftest + real oauth-login (slot-isolated storageState persistence). See docs/oauth-login.md."
entry = "bin/browser-harness"
license = "MIT"

[package.dependencies]
node = ">=18.0.0"

[package]
name = "browser-harness"
version = "0.1.0"
description = "Playwright-based browser automation harness for need-singularity fleet. Fresh-context factory (mandate 1-6) + probe + selftest. v0.1.0 = install land + structural selftest; oauth-login = forward-spec stub."
entry = "bin/harness"
license = "MIT"

[package.dependencies]
node = ">=18.0.0"
